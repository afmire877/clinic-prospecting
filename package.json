{
  "name": "mdm-app-scheduler",
  "version": "1.0.0",
  "description": "Generate iOS mobileconfig profiles to block apps on a schedule",
  "main": "dist/cli.js",
  "bin": {
    "mdm-scheduler": "./bin/mdm-scheduler"
  },
  "scripts": {
    "build": "tsc",
    "dev": "ts-node src/cli.ts",
    "start": "node dist/cli.js"
  },
  "keywords": ["mdm", "ios", "mobileconfig", "app-blocker", "schedule"],
  "license": "MIT",
  "dependencies": {
    "commander": "^11.1.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/uuid": "^9.0.7",
    "ts-node": "^10.9.1",
    "typescript": "^5.3.0"
  }
}
